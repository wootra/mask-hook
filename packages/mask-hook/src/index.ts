export { useMask } from './useMask';
export type { UseMaskOptions, UseMaskResult, MaskType } from './types';
export {
  formatEIN,
  formatSSN,
  maskEIN,
  maskSSN,
  stripNonDigits,
  TOTAL_DIGITS,
} from './formatters';
